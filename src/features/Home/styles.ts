import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    loadingContainer: {
        justifyContent: 'center',
        alignContent: 'center',
        flex: 1
    },
    noMoreContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1
    },
    noMoreText: {
        marginBottom: 20
    }
})